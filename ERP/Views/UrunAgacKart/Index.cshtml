@model IEnumerable<ERP.Models.Veri.UrunAgac.BSMGRNNMBOMHEAD>

@{
    ViewData["Title"] = "Ürün Ağacı";
}

<h1 class="text-primary text-center mb-4">Ürün Ağacı</h1>

<div class="d-flex justify-content-end mb-3">
    <a class="btn btn-success btn-lg shadow-sm" asp-action="Create">
        <i class="fas fa-plus-circle"></i> Yeni Ürün Ağacı Oluştur
    </a>
</div>

<table class="table table-striped table-hover table-bordered shadow-sm">
    <thead class="table-dark">
        <tr>
            <th>Firma Kodu</th>
            <th>Ürün Ağacı Kodu</th>
            <th>Ürün Ağacı Tipi</th>
            <th>Geçerlilik Başlangıç</th>
            <th>Geçerlilik Bitiş</th>
            <th>Malzeme Kodu</th>
            <th>Malzeme Tipi</th>
            <th>İçerik Numarası</th>
            <th>Bileşen</th>
            <th>Silindi mi?</th>
            <th>Pasif mi?</th>
            <th>Çizim Numarası</th>
            <th class="text-center">İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.COMCODE)</td>
                <td>@Html.DisplayFor(modelItem => item.BOMDOCNUM)</td>
                <td>@Html.DisplayFor(modelItem => item.BOMDOCTYPE)</td>
                <td>@Html.DisplayFor(modelItem => item.BOMDOCFROM)</td>
                <td>@Html.DisplayFor(modelItem => item.BOMDOCUNTIL)</td>
                <td>@Html.DisplayFor(modelItem => item.MATDOCNUM)</td>
                <td>@Html.DisplayFor(modelItem => item.MATDOCTYPE)</td>
                <td>@Html.DisplayFor(modelItem => item.CONTENTNUM)</td>
                <td>@Html.DisplayFor(modelItem => item.COMPONENT)</td>
                <td>@Html.DisplayFor(modelItem => item.ISDELETED)</td>
                <td>@Html.DisplayFor(modelItem => item.ISPASSIVE)</td>
                <td>@Html.DisplayFor(modelItem => item.DRAWNUM)</td>
                <td class="text-center">
                    <a class="btn btn-primary btn-sm mx-1 shadow-sm" asp-action="Edit" asp-route-id="@item.BOMDOCNUM">
                        <i class="fas fa-edit"></i> Düzenle
                    </a>
                    <a class="btn btn-warning btn-sm mx-1 shadow-sm" asp-action="Details" asp-route-id="@item.BOMDOCNUM">
                        <i class="fas fa-info-circle"></i> Detay
                    </a>
                    <a class="btn btn-danger btn-sm mx-1 shadow-sm" asp-action="Delete" asp-route-id="@item.BOMDOCNUM">
                        <i class="fas fa-trash-alt"></i> Sil
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

<style>
    .table-hover tbody tr:hover {
        background-color: #f1f1f1;
        transition: background-color 0.3s ease;
    }

    .btn {
        transition: all 0.2s ease-in-out;
    }

        .btn:hover {
            transform: scale(1.05);
        }
</style>
